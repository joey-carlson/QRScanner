<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Bluetooth HID Device permissions for Live Scan feature -->
    <!-- Android 12+ (API 31+): fine-grained BT permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <!-- Android 9-11 (API 28-30): legacy BT permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- BT hardware feature — not required so app installs on non-BT devices -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <application
        android:name=".QRScannerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QRScanner"
        tools:targetApi="31">
        <!-- Home Activity - Main launcher -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- MainActivity - Check Out feature -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".HomeActivity" />
        
        <!-- CheckInActivity - Check In feature -->
        <activity
            android:name=".CheckInActivity"
            android:exported="false"
            android:label="@string/check_in_mode_title"
            android:screenOrientation="portrait"
            android:parentActivityName=".HomeActivity" />
            
        <!-- UserCheckInActivity - User Check In feature (placeholder) -->
        <activity
            android:name=".UserCheckInActivity"
            android:exported="false"
            android:label="@string/user_check_in_mode_title"
            android:screenOrientation="portrait"
            android:parentActivityName=".HomeActivity" />
            
        <!-- KitBundleActivity - Kit Bundle feature -->
        <activity
            android:name=".kitbundle.KitBundleActivity"
            android:exported="false"
            android:label="@string/kit_bundle_mode_title"
            android:screenOrientation="portrait"
            android:parentActivityName=".HomeActivity" />
            
        <!-- InventoryManagementActivity - Inventory Management feature -->
        <activity
            android:name=".inventory.InventoryManagementActivity"
            android:exported="false"
            android:label="@string/inventory_management_title"
            android:screenOrientation="portrait"
            android:parentActivityName=".HomeActivity" />
            
        <!-- UnifiedExportActivity - Centralized export/share functionality -->
        <activity
            android:name=".export.UnifiedExportActivity"
            android:exported="false"
            android:label="Export Data"
            android:screenOrientation="portrait"
            android:parentActivityName=".HomeActivity" />
            
        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:screenOrientation="portrait" />
        
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>
